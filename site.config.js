const siteConfig = {
  siteName: "Cordova Public College",
  siteUrl: process.env.NEXT_PUBLIC_TENANT_API,
  siteImagePath: `/_next/image?q=100&url=${process.env.NEXT_PUBLIC_TENANT_API}`,
};

export default siteConfig;
